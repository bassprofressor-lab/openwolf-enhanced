- OpenWolf manages this project's memory. The full protocol is in `.wolf/OPENWOLF.md`, which
  CLAUDE.md already imports — follow that file; it is the single source, not this one.
- Read `.wolf/STATUS.md` first when resuming. Check `.wolf/anatomy.md` before opening a file, and
  `.wolf/buglog.json` before fixing one.
- Record what you learn: corrections and conventions to `.wolf/cerebrum.md`, actions to
  `.wolf/memory.md`, every bug or failed command to `.wolf/buglog.json`.
